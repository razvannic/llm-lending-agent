meta {
  name: Retrieve terms and conditions acceptance status
  type: http
  seq: 6
}

get {
  url: {{baseUrl}}/api/persons/{{personId}}/terms?accountProductId={{savingsProductId}}
  body: json
  auth: inherit
}

params:query {
  accountProductId: {{savingsProductId}}
}

headers {
  Accept: application/json
}

docs {
  List all terms and conditions documents and their associated versions for an account product.
}
