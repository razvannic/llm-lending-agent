meta {
  name: Mark documents for a lending application action as signed
  type: http
  seq: 4
}

put {
  url: {{baseUrl}}/api/persons/:personId/lending-applications/:lendingApplicationId/document-orchestration-actions/:actionId/sign
  body: json
  auth: inherit
}

params:path {
  personId: {{personId}}
  lendingApplicationId: {{lendingApplicationId}}
  actionId: {{actionId}}
}

headers {
  Content-Type: application/json
  Accept: application/json
}

body:json {
  {
    "documentIds": {{documentIds}}
  }
}

docs {
  Applicant documents associated to a lending application action must be marked as viewed and signed in order for pending action of type VIEW_AND_SIGN_DOCUMENTS to be completed.
  
  Use this endpoint to mark applicant documents associated to a lending application action as signed.
  
  Every document associated to the action must be marked as signed at the same time. Signing can occur only once.
  
  :::warning
   This endpoint is not yet implemented and subject to change.
  :::
  
}
