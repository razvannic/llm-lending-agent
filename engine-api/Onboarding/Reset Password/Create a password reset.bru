meta {
  name: Create a password reset
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/api/persons/:personId/password/reset
  body: json
  auth: inherit
}

params:path {
  personId: {{personId}}
}

headers {
  x-device-id: {{deviceId}}
}

body:json {
  {
    "password": "JakeTheSnake"
  }
}

script:post-response {
  let responseData = res.getBody();
  bru.setVar("reviewId", responseData.reviewId);
  
}
