meta {
  name: Retrieve terms and conditions acceptance status
  type: http
  seq: 7
}

get {
  url: {{baseUrl}}/api/persons/{{personId}}/terms?accountProductId={{loanProductId}}
  body: json
  auth: inherit
}

params:query {
  accountProductId: {{loanProductId}}
}

headers {
  Accept: application/json
}

docs {
  List all terms and conditions documents and their associated versions for an account product.
}
